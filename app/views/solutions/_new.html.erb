

<!-- Original Code -->
		
		<%= form_for([@course,
		 @solution],
		 :html => { :multipart => true }) do |f| %>
			<%= render 'shared/error_messages', object: f.object %>

      		<%= f.file_field :answer, class: 'form-control' %><br>

			<%= f.submit "Upload", class: "btn btn-primary" %>
    	<% end %><br>
<%= "Note: Only PDF, EXCEL, WORD, TEXT or Image(png, gif, jpeg) files are allowed." %>
