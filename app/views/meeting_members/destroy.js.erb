$(".attendance").html("<%= escape_javascript(render partial: 'meetings/attendance', locals: { meeting: @meeting} ) %>");
$(".attending").html("<% @meeting.meeting_members.each do |member|  %>
                                          <%= User.find_by_id(member.user_id).name %><br>
                                    <% end %>");